# Local Python install used only for offline testing — never commit
.libs/
__pycache__/
*.pyc

# Generated output (GitHub builds this fresh every run)
site/

# Caches are handled by GitHub's cache, not the repo
.translation_cache.json
.classify_cache.json
